<%= form_with(model: current_user.following_relationships.build, remote: true) do |f| %>
  <%= f.hidden_field :following_id, value: @user.id %>
  <%= f.submit "フォロー", class: "btn btn-success w-130px" %>
<% end %>