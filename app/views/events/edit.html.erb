<div class="body">
  <div class="container">
    <div class="row">
      <h1>編集ページ</h1>
       <%= render 'form', event: @event %>
       <%= link_to 'Show', user_event_path(current_user,@event) %> |
       <%= link_to 'Back', user_events_path(current_user) %> |
    </div>
  </div>
</div>