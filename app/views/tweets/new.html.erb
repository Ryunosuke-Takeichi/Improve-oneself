<div class="container">
  <div class="row">
    <%= form_with model: @tweet, rocal: true do |f| %>
      <%= f.label :body, 'ツイート内容' %>
      <%= f.text_field :body %>
  </div>
  
  <div class="action_row">
      <%= f.submit "送信" %>
    </div>
    <% end %>
</div>